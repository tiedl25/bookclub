import 'package:flutter/material.dart';

Color bookSelectedColor = Colors.brown.shade400;
Color bookDefaultColor = Colors.grey.shade400;

Color backgroundColor = Color.fromARGB(255, 240, 237, 221);
Color themeColor = Color.fromARGB(255, 212, 210, 185);

ColorScheme colorScheme = ColorScheme.fromSeed(
  seedColor: themeColor,
  background: backgroundColor
  
);
