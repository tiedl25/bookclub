import 'package:flutter/material.dart';

Color bookSelectedColor = Colors.brown.shade400;
Color bookDefaultColor = Colors.grey.shade400;

Color backgroundColor = Color.fromARGB(255, 242, 240, 227);
Color themeColor = Color.fromARGB(255, 18, 65, 135);

ColorScheme colorScheme = ColorScheme.fromSeed(
  seedColor: themeColor,
  surface: backgroundColor
  
);
